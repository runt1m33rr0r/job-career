image: node:12.13.0
 
pages:
  cache:
    paths:
      - ./node_modules/
 
  stage: deploy
  script:
    - npm install
    - npm run build
    - mv build/* public/
  artifacts:
    paths:
      - public
  only:
    - development-ui
